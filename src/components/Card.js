

const Card = (props) => {
    return(
                <div className='card'> 
                    <img className='image' src={props.image} alt='' />
                </div>
    )
}

export default Card;